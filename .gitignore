# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Harness Wasm (fetched at build from Actions artifact — do not commit)
public/harness/harness.wasm
public/harness/web.wasm
public/harness/web.js
public/harness/index.html
public/harness/.artifact-meta.json

# Local sandbox workspace (dev)
.sandbox-workspace/

# Agent exec on-disk logs (written at the workspace root by the exec tool — never commit)
/.invincible/

# Zig local package extracts / caches (agent workspaces)
native/harness/zig-pkg/
native/harness/zig-cache/
native/harness/.zig-cache/
native/harness/zig-out/
native/dist/
/.swc
